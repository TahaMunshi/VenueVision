# Web framework
Flask==3.0.0
python-dotenv==1.0.1
flask-cors==4.0.0
flask-compress==1.15

# Database
psycopg2-binary==2.9.9

# Image/3D processing
numpy==1.26.4
opencv-python==4.10.0.84
scikit-image>=0.21.0
# opencv-contrib-python includes Stitcher; use it instead of opencv-python if Stitcher is needed:
# pip install opencv-contrib-python  (replaces opencv-python, do not install both)
pygltflib==1.16.2

# Auth
PyJWT==2.8.0
bcrypt==4.1.2
Pillow==10.2.0
trimesh==4.0.8
# HTTP client (used by Docker healthcheck and optional scripts)
requests==2.31.0
# Tripo3D: upload to S3 with temporary STS credentials
boto3>=1.34.0
gradio_client>=1.4.0
