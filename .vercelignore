# Extra exclusions for upload packaging (optional). Vercel no longer needs root
# requirements.txt — deps live in requirements.docker.txt for Docker only.
server/
Dockerfile
docker-compose.yml
docker-compose.dev-frontend.yml
render.yaml
venuevision-inpaint/
